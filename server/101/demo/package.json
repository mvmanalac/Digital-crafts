{
  "name": "demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}

// dependencies/express are needed for the application to work
// devDependencies/nodemon are needed for the application itself
// nodemon restarts the server when we make changes
// npm install -D nodemon in Terminal adds "devDependencies"
// npx nodemon server.js - dunno what this does, but you run it after making changes in server.js
