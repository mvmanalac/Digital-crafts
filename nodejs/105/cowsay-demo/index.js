// cowsay example

var cowsay = require("cowsay");

console.log(cowsay.say({
    text : "I'm a moooodule",
    e : "oO",
    T : "U "
}));

// or cowsay.think()

// in node: 

// node index.js (this is running the file this notes are on. like the actual notes you're reading from)